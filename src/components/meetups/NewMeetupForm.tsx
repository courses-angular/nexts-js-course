import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const NewMeetupForm: FunctionComponent<Props> = (props) => {
  return <></>;
};

export default NewMeetupForm;
